[package]
name = "schule"
version = "0.1.1"
entrypoint = "src/schule.typ"
authors = ["J. Neugebauer"]
license = "MIT"
description = "Arbeitsblätter und Klausuren."
repository = "https://github.com/jneug/schule-typst"
exclude = ["docs/*"]
compiler = "0.8.0"
